{% extends 'base.html.twig' %}

{% block title %}Libro{% endblock %}

{% block body %}

    <div class="col-8 offset-2 mt-5">
        <div class="card">
            <div class="card-header">
                <h6 class="float-start pt-1"><i class="fa-solid fa-book"></i>&nbsp;&nbsp;Detalles de libro</h6>
            </div>
            <div class="card-body">
                <table class="table">
                    <tbody>
                        <tr>
                            <th>Código</th>
                            <td>{{ libro.id }}</td>
                        </tr>
                        <tr>
                            <th>Titulo</th>
                            <td>{{ libro.titulo }}</td>
                        </tr>
                        <tr>
                            <th>Autor</th>
                            <td>{{ libro.autor }}</td>
                        </tr>
                        <tr>
                            <th>Descripcion</th>
                            <td>{{ libro.descripcion }}</td>
                        </tr>
                        
                    </tbody>
                </table>
                <div class="row">
                    <div class="col-12">
                        <a class="btn btn-danger float-start" href="{{ path('app_libro_index') }}"><i class="fa-solid fa-list"></i>&nbsp;&nbsp;Volver al listado</a>
                    </div>
                </div>
            </div>
        </div>
    </div>


{% endblock %}
